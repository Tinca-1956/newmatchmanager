rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // WARNING: INSECURE - This is a temporary rule for debugging.
    // It allows any authenticated user to read/write all data.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
