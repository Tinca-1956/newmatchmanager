
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    // Default allow read for all collections for authenticated users.
    // WARNING: This is for debugging only and is not secure for production.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
