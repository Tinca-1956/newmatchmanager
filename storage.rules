
rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow read, write, delete access to any file for any authenticated user.
    match /{allPaths=**} {
      allow read, write, delete: if request.auth != null;
    }
  }
}
