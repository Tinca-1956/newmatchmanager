rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow read and write access to any file if the user is signed in.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
